spring:
  application:
    name: Storage Service

  devtools:
    restart:
      enabled: true
      poll-interval: 2s
      quiet-period: 1s
    livereload: 
      enabled: true

minio:
  url: http://minio:9000
  access:
    name: minioadmin
    secret: minioadmin
  bucket:
    name: data
    
